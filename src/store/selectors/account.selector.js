// eslint-disable-next-line import/prefer-default-export
export const accountListSelector = (state) => state.account.accounts

export const remarksSelector = (state) => state.account.remarks

export const selectedAccountDetailsSelector = (state) => state.account.currentItem

export const isAccRefreshSelector = (state) => state.account.isAccRefresh

export const debitListSelector = (state) => state.account.debitList


